### Installation

Check `Makefile` for more

```
make setup
```

### Use Existing Data

tar -xvzf data.tar.gz

```
from app.services.shopify_app_analyzer_service import ShopifyAppAnalyzerService

ShopifyAppAnalyzerService().data()
```

### Crawling

`make crawl`
